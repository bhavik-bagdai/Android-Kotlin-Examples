fun main() {
    val fruits = arrayOf("Apple", "Banana", "Cherry")
    for (fruit in fruits) {
        println(fruit) // Prints each fruit in the array
    }

    for (i in 1..5) {
        println("i = $i") // Prints numbers from 1 to 5
    }
}